pre: code .
windows:
  - name: Hexo AUCCoder
    root: ~/Sites/hexo_auxcoder_io/
    layout: main-vertical
    panes:
      - commands:
          - cd themes/anatole
          - l
          - git pull
          - git status
        focus: true
        name: "theme folder repo"
      - commands:
          - git pull
          - git status
        name: "git"
      - commands:
          - hexo server --draft
        name: "dev server"
